export function addToCart(item) {
    console.log(item + " added to cart.");
}

export function removeFromCart(item) {
    console.log(item + " removed from cart.");
}

export function calculateTotal(price1, price2) {
    return price1 + price2;
}